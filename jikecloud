cp /data/inputs.zip inputs.zip
unzip inputs.zip -d inputs > /dev/null
rm inputs.zip
git clone https://github.com/aboluo11/lightai.git
git clone https://github.com/aboluo11/salt.git
mv salt salt_source
ln -s salt_source/salt salt
pip install tensorboardX
mkdir model